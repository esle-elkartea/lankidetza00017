package ${package}.app.event;

import net.zylk.web.listener.BaseServletContextListener;
import java.util.logging.Logger;

/**
#if ($addTimeStamp)
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [$now]
 *
#end
 *  You should add additional methods to this class to meet the
 *  application requirements.  This class will only be generated as
 *  long as it does not already exist in the output directory.
 */
public class OnStartUp extends BaseServletContextListener
{
 private static final Logger logger = Logger.getLogger("${package}.app.event.OnStartUp");

 #set ($pkConfigFilePath ="${package}.app.config")
 #set ($strPath = "$strings.getPackageAsPath(${pkConfigFilePath})app-config")
 public String getConfigFilePath() {
  return "$strPath.replaceAll("\\","/")";
 }
}


