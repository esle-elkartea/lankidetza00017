package ${package}.manager;

import org.apache.torque.TorqueException;
import org.apache.torque.util.Criteria;

import ${package}.model.${table.JavaName}Peer;

/**
#if ($addTimeStamp)
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [$now]
 *
#end
 *  You should add additional methods to this class to meet the
 *  application requirements.  This class will only be generated as
 *  long as it does not already exist in the output directory.
 */
public class ${table.JavaName}Manager
    extends ${basePrefix}${table.JavaName}Manager
{


//Borrado de un elemento
#set ($pks = $table.PrimaryKey)
#if ($pks.size() == 1)
  #set ($pk = $pks.get(0))
  #set ( $cup=$pk.Name.toUpperCase() )
#end
	public static boolean borra${table.JavaName}(int idBorrar) {

		Criteria c=new Criteria();
		c.add(${table.JavaName}Peer.${cup}, idBorrar);
		boolean bRet = false;
		try {
			${table.JavaName}Peer.doDelete(c);
			bRet = true;
		} catch (TorqueException e) {
			logger.info("No se ha podido borrar en la tabla ${table.JavaName}");
			e.printStackTrace();
			bRet = false;
		}
		return bRet;
	}

}


